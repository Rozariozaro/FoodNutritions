# Implementation Plan: Design System Adoption

**Branch**: `002-design-system-adoption` | **Date**: 2026-03-17 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/002-design-system-adoption/spec.md`

## Summary

Replace all hardcoded colour, typography, spacing, radius, and shadow literals across 11 `Core/UI` components and 5 feature screens with references to the existing `AppTheme` design token system (`AppColors`, `AppTypography`, `AppSpacing`, `AppRadius`, `AppShadows`). Add one missing token (`AppSpacing.smMd = 12`) to complete the constitution-defined 4pt grid scale. No behaviour changes, no new features, no data model changes beyond the single token addition.

## Technical Context

**Language/Version**: Swift 5.9+
**Primary Dependencies**: SwiftUI (Apple framework only)
**Storage**: N/A (no persistence changes)
**Testing**: XCTest (existing; no new unit tests required — refactor is styling-only with no logic changes)
**Target Platform**: iOS 17+
**Project Type**: Mobile app (SwiftUI)
**Performance Goals**: No regression in scroll performance or render speed; token lookups are compile-time constants
**Constraints**: Zero API/behaviour changes; all existing call sites remain backward-compatible; no third-party packages
**Scale/Scope**: 11 `Core/UI` components + 5 feature screens + 1 token addition + minimal demo update

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-checked after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| MVI architecture (§3.1) | ✅ Pass | Refactor touches only View styling; no Processor/State/Intent changes |
| SwiftUI only (§2.3) | ✅ Pass | All changes are SwiftUI modifier replacements |
| Swift Concurrency (§2.4) | ✅ Pass | No async code added or removed |
| Layer boundaries (§4.1) | ✅ Pass | `Core/DesignTokens` is part of the Core layer; changes stay within Core and Features |
| Reusable components in `Core/UI` (§9.2) | ✅ Pass | Feature of this refactor — strengthens compliance |
| 4pt grid system (§9.1) | ✅ Pass | Adding `AppSpacing.smMd = 12` restores the missing constitution-defined grid step |
| SwiftLint compliance (§12) | ✅ Pass | Token references are shorter/simpler than raw literals; no new violations expected |
| No business logic in Views (§12) | ✅ Pass | Styling changes only |
| File length ≤ 300 lines (§12) | ✅ Pass | No file grows; most shrink or stay same length |

**Post-design re-check**: No violations. One additive change (`AppSpacing.smMd`) aligns with constitution §9.1 rather than violating it.

**Complexity Tracking**: No violations requiring justification.

## Project Structure

### Documentation (this feature)

```text
specs/002-design-system-adoption/
├── plan.md              ← this file
├── research.md          ← Phase 0 complete
├── data-model.md        ← Phase 1 complete
├── spec.md              ← source spec
├── checklists/
│   └── requirements.md
└── tasks.md             ← generated by /speckit.tasks
```

### Source Code (affected paths only)

```text
FoodNutritions/FoodNutritions/FoodNutritions/
├── Core/
│   ├── DesignTokens/
│   │   ├── AppSpacing.swift            ← ADD smMd = 12
│   │   ├── MacroProgressBar.swift      ← add cross-ref comment
│   │   ├── CircularProgressView.swift  ← add cross-ref comment
│   │   ├── ListItemRow.swift           ← add cross-ref comment
│   │   └── DesignSystemDemoView.swift  ← minimal showcase update
│   └── UI/
│       ├── FoodCard.swift              ← tokenise
│       ├── CalorieRingView.swift       ← tokenise
│       ├── MacroProgressBar.swift      ← tokenise + cross-ref comment
│       ├── MealItemRow.swift           ← tokenise + cross-ref comment
│       ├── CalendarRibbon.swift        ← tokenise
│       ├── NutritionSummaryView.swift  ← tokenise
│       ├── NutritionMacroHeader.swift  ← tokenise
│       ├── ErrorBanner.swift           ← verify/tokenise
│       ├── FoodDetailSection.swift     ← verify/tokenise
│       ├── MicronutrientListView.swift ← verify/tokenise
│       └── SortMenuView.swift          ← verify/tokenise
└── Features/
    ├── Dashboard/DashboardView.swift   ← tokenise
    ├── Search/SearchView.swift         ← tokenise
    ├── FoodDetail/FoodDetailView.swift ← tokenise
    ├── MealLog/MealLogView.swift       ← tokenise
    └── History/HistoryView.swift       ← tokenise
```

**Structure Decision**: Single iOS project. All changes are within the existing source tree. No new files created.

## Implementation Phases

### Phase A: Token Foundation

**Goal**: Ensure the token system is complete before components reference it.

**A1** — Add `AppSpacing.smMd: CGFloat = 12` to `Core/DesignTokens/AppSpacing.swift`.
- Insert after `sm` line: `public static let smMd: CGFloat = 12`
- Verify `AppTheme.spacing.smMd` resolves at call sites.

---

### Phase B: Core/UI Component Tokenisation

Each task is independent and can be completed in any order after Phase A.

**B1 — FoodCard.swift**
- `spacing: 8` → `AppSpacing.sm`; `spacing: 2` → `AppSpacing.xs`; `spacing: 16` → `AppSpacing.md`
- `.font(.headline)` → `AppTypography.headline`
- `.font(.caption)` / `.font(.caption2)` → `AppTypography.caption1`
- `.font(.subheadline.bold())` → `AppTypography.subhead.bold()`
- `.font(.caption2.bold())` → `AppTypography.caption1.bold()`
- `.foregroundStyle(.secondary)` → `.foregroundStyle(AppColors.textSecondary)`
- `color: .blue` → `AppColors.protein`; `color: .green` → `AppColors.carbs`; `color: .orange` → `AppColors.fat`
- `.padding(12)` → `.padding(AppSpacing.smMd)`
- `.background(.background)` → `.background(AppColors.surface)`
- `cornerRadius: 12` → `AppRadius.md`
- `.shadow(color: .black.opacity(0.06), radius: 4, x: 0, y: 2)` → `.appShadowSoft()`

**B2 — CalorieRingView.swift**
- Default `ringColor` from `.orange` → `AppColors.primary`
- `isOverGoal ? .red : ringColor` → `isOverGoal ? AppColors.error : ringColor`
- Conditional font expressions: replace `.subheadline.bold()` → `AppTypography.subhead.bold()`; `.title2.bold()` → `AppTypography.title2.bold()`; `.caption2` → `AppTypography.caption1`; `.caption` → `AppTypography.footnote`
- `.foregroundStyle(.secondary)` → `.foregroundStyle(AppColors.textSecondary)`
- `.foregroundStyle(isOverGoal ? .red : .primary)` → `.foregroundStyle(isOverGoal ? AppColors.error : AppColors.textPrimary)`

**B3 — Core/UI/MacroProgressBar.swift**
- `spacing: 4` → `AppSpacing.xs`
- `.font(.subheadline.weight(.medium))` → `AppTypography.subhead.weight(.medium)`
- `.font(.caption)` → `AppTypography.caption1`
- `.foregroundStyle(.secondary)` → `.foregroundStyle(AppColors.textSecondary)`
- Default `color` parameter: `.blue` → `AppColors.protein`
- Add cross-reference comment: `// See also: Core/DesignTokens/MacroProgressBar.swift (simpler value/color/label API)`

**B4 — MealItemRow.swift**
- `spacing: 2` (both VStacks) → `AppSpacing.xs`
- `.font(.headline)` → `AppTypography.headline`
- `.font(.subheadline)` → `AppTypography.subhead`
- `.font(.subheadline.bold())` → `AppTypography.subhead.bold()`
- `.font(.caption2)` → `AppTypography.caption1`
- `.foregroundStyle(.secondary)` → `.foregroundStyle(AppColors.textSecondary)`
- Add cross-reference comment: `// See also: Core/DesignTokens/ListItemRow.swift (generic HStack layout wrapper)`

**B5 — CalendarRibbon.swift**
- `Color(.systemBackground)` (ribbon bg) → `AppColors.background`
- `spacing: 12` (date HStack) → `AppSpacing.smMd`
- `.padding(.vertical, 8)` → `.padding(.vertical, AppSpacing.sm)`
- `spacing: 4` (DateCell VStack) → `AppSpacing.xs`
- `.font(.caption2.weight(.medium))` → `AppTypography.caption1.weight(.medium)`
- `.font(.headline)` → `AppTypography.headline`
- `.foregroundStyle(.secondary)` → `AppColors.textSecondary`; `.foregroundStyle(.primary)` → `AppColors.textPrimary`
- `cornerRadius: 12` (all three RoundedRectangle) → `AppRadius.md`
- `Color.blue.gradient` → `AppColors.primary.gradient` (retain gradient modifier)
- `Color.blue` (today stroke) → `AppColors.primary`
- `Color(.secondarySystemBackground)` → `AppColors.surface`

**B6 — NutritionSummaryView.swift**
- `spacing: 12` → `AppSpacing.smMd`; `spacing: 8` → `AppSpacing.sm`
- `.font(.title.bold())` → `AppTypography.title1.bold()`
- `.font(.caption)` → `AppTypography.caption1`
- `.foregroundStyle(.secondary)` → `AppColors.textSecondary`
- `.padding(.vertical, 8)` → `.padding(.vertical, AppSpacing.sm)`
- `MacroProgressBar(color: .blue)` → `AppColors.protein`; `.green` → `AppColors.carbs`; `.orange` → `AppColors.fat`

**B7 — NutritionMacroHeader.swift**
- `spacing: 32` → `AppSpacing.xl`; `spacing: 12` → `AppSpacing.smMd`; `spacing: 4` → `AppSpacing.xs`
- `.font(.caption.bold())` → `AppTypography.caption1.bold()`; `.font(.caption)` → `AppTypography.caption1`
- `.foregroundStyle(.secondary)` → `AppColors.textSecondary`
- `.padding()` → `.padding(AppSpacing.md)`
- `Color(.secondarySystemBackground)` → `AppColors.surface`
- `cornerRadius: 20` → `AppRadius.lg`
- `color: .blue/.green/.orange` in `macroRow` → `AppColors.protein/.carbs/.fat`
- `ringColor: .orange` → omit (use updated `CalorieRingView` default of `AppColors.primary`)

**B8–B11 — ErrorBanner, FoodDetailSection, MicronutrientListView, SortMenuView** (verify)
- Audit each file for any remaining hardcoded literals; apply token replacements if found.

---

### Phase C: Feature Screen Tokenisation

Each task is independent. Depends on Phase B (components should be tokenised first so calling conventions are finalised).

**C1 — DashboardView.swift**
- `.padding(.vertical, 8)` → `AppSpacing.sm`
- `EdgeInsets(top: 16, leading: 16, bottom: 16, trailing: 16)` → `AppSpacing.md` for all four edges
- `Color(.systemBackground)` → `AppColors.background`
- `Color.clear.frame(height: 80)` → `Color.clear.frame(height: AppSpacing.xxl + AppSpacing.xl)`
- `.font(.title2.bold())` (FAB) → `AppTypography.title2.bold()`
- `Color.blue` (FAB fill) → `AppColors.primary`
- `.shadow(color: .black.opacity(0.3), radius: 8, x: 0, y: 4)` → `.appShadowSoft()`
- `.padding(24)` (FAB) → `.padding(AppSpacing.lg)`

**C2 — SearchView.swift**
- `spacing: 8` (searchBar HStack) → `AppSpacing.sm`
- `.padding(10)` → `.padding(AppSpacing.sm)`
- `.background(.quaternary)` → `.background(AppColors.surface)`
- `cornerRadius: 12` (search field) → `AppRadius.md`
- `.padding(.vertical, 8)` → `AppSpacing.sm`
- `.padding(.horizontal, 16)` / `.padding(.leading, 16)` → `AppSpacing.md`
- `.padding(.vertical, 12)` → `AppSpacing.smMd`
- `.background(.regularMaterial)` (autocomplete) → `AppColors.surfaceElevated`
- `cornerRadius: 12` (autocomplete) → `AppRadius.md`
- `.shadow(color: .black.opacity(0.12), radius: 8, x: 0, y: 4)` → `.appShadowSoft()`
- `.padding(.top, 4)` → `AppSpacing.xs`
- `spacing: 8` (recentSearches) → `AppSpacing.sm`
- `.font(.subheadline.bold())` → `AppTypography.subhead.bold()`
- `.foregroundStyle(.secondary)` → `AppColors.textSecondary`
- `.padding(.top, 16)` → `AppSpacing.md`
- `spacing: 12` (resultsSection) → `AppSpacing.smMd`
- `.font(.subheadline)` → `AppTypography.subhead`
- `.padding(.top, 8)` → `AppSpacing.sm`
- `spacing: 16` (emptyState) → `AppSpacing.md`
- `.padding(.top, 40)` → `AppSpacing.xl`
- `.font(.caption)` (autocomplete label) → `AppTypography.caption1`

**C3 — FoodDetailView.swift**
- `spacing: 24` → `AppSpacing.lg`; `spacing: 8` → `AppSpacing.sm`; `spacing: 4` → `AppSpacing.xs`
- `.font(.largeTitle.bold())` → `AppTypography.largeTitle.bold()`
- `.font(.headline)` → `AppTypography.headline`
- `.font(.caption)` → `AppTypography.caption1`
- `.font(.subheadline)` → `AppTypography.subhead`
- `.foregroundStyle(.secondary)` → `AppColors.textSecondary`
- `Color.blue` (button enabled) → `AppColors.primary`
- `Color.gray` (button disabled) → `AppColors.textSecondary`
- `cornerRadius: 15` → `AppRadius.lg`
- `.padding()` (button) → `.padding(AppSpacing.md)`

**C4 — MealLogView.swift**
- `cornerRadius: 12` (saving overlay) → `AppRadius.md`
- `.background(.ultraThinMaterial)` → `.background(AppColors.surfaceElevated)`
- `.padding()` (saving overlay) → `.padding(AppSpacing.md)`

**C5 — HistoryView.swift**
- `EdgeInsets(top: 8, leading: 0, bottom: 8, trailing: 0)` → `EdgeInsets(top: AppSpacing.sm, leading: 0, bottom: AppSpacing.sm, trailing: 0)`

---

### Phase D: Cross-Reference Comments & Demo Update

**D1 — Add cross-reference comments to Core/DesignTokens components**
- `Core/DesignTokens/MacroProgressBar.swift`: `// See also: Core/UI/MacroProgressBar.swift (richer current/goal progress API for dashboard/summary use)`
- `Core/DesignTokens/CircularProgressView.swift`: `// See also: Core/UI/CalorieRingView.swift (labelled calorie ring with goal text and over-goal state)`
- `Core/DesignTokens/ListItemRow.swift`: `// See also: Core/UI/MealItemRow.swift (food-specific row with macro detail)`

**D2 — DesignSystemDemoView.swift (minimal update)**
- Audit current demo; add showcase rows for any token-aware components not yet represented.
- Do not restructure or reorder existing content.

---

## Verification Checklist (post-implementation, validates SC-001–SC-003)

After all phases complete, run source audits:

```bash
# SC-001: zero raw colour literals
grep -rn "\.blue\b\|\.green\b\|\.orange\b\|\.red\b\|Color(\.system\|secondarySystem\|\.quaternary\|regularMaterial\|ultraThinMaterial" \
  FoodNutritions/FoodNutritions/FoodNutritions/Core/UI \
  FoodNutritions/FoodNutritions/FoodNutritions/Features

# SC-002: zero raw numeric spacing/radius/shadow literals
grep -rn "\.padding([0-9]\|spacing: [0-9]\|cornerRadius: [0-9]\|\.shadow(color:" \
  FoodNutritions/FoodNutritions/FoodNutritions/Core/UI \
  FoodNutritions/FoodNutritions/FoodNutritions/Features

# SC-003: zero system font literal calls
grep -rn "\.font(\.\(headline\|subheadline\|caption\|largeTitle\|title\|body\|footnote\))" \
  FoodNutritions/FoodNutritions/FoodNutritions/Core/UI \
  FoodNutritions/FoodNutritions/FoodNutritions/Features
```

Expected: zero matches (exceptions: `#Preview` blocks and inline comments are acceptable).
